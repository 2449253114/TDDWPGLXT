# 扩展数据库隔离迁移调研与方案

> 文档状态：待审核，不代表已获准实施
>
> 调研日期：2026-08-20
>
> 本轮范围：仅审计 `uniCloud-aliyun/cloudfunctions`，未审计 `pages`，未修改可执行代码，未创建新 uni 项目，未迁移、上传或部署任何线上资源。

## 1. 结论

你的总体思路可行，而且适合作为当前项目最保守的迁移路线：

1. 保持现有线上项目、线上云函数和内置数据库不动。
2. 新建一个独立 uni 项目和独立服务空间，授权它访问已经购买的扩展数据库。
3. 在新项目中保持现有集合名、字段名、字段类型、`_id` 及关联关系不变，重新实现相同 URL 和响应契约的云对象 API。
4. 先把内置数据库的快照迁入扩展数据库，只在隔离环境验证。
5. 第一条试点链路使用客户端 App 的登录接口：`POST /api/users/oaid-login-v2`。
6. 登录及其依赖验证通过后，再按风险从低到高迁移其他 App API。
7. 正式切换前执行最终数据同步和停写控制，避免测试快照与线上数据产生差异。

这条路线降低的是“直接改坏现有线上云函数”的风险，但它不会自动解决以下风险：

- 测试快照与持续写入的线上内置数据库之间会越来越不一致。
- 扩展数据库兼容 `uniCloud.database()` 默认 API，但阿里云内置库独有的附加返回字段不能继续依赖。
- 登录本身会更新登录时间、VIP 状态，也可能注册新用户，不是只读接口。
- 新旧入口同时接收写请求时会形成双库数据分叉。
- 定时任务、支付回调如果新旧两边同时运行，可能重复更新数据或重复发放权益。
- 一旦扩展数据库已经接收新写入，不能简单把 URL 切回旧库，否则会丢失新库期间的数据。

因此，推荐采用“隔离验证、分接口放量、最终短暂停写切换”的方案，不推荐一开始就在现有线上项目中直接启用扩展数据库，也不推荐长期双写。

## 2. 已确认的目标与边界

### 2.1 已确认

- 目标是把客户端 App 使用的云函数/云对象 API 的数据源，从 uniCloud 内置数据库迁到扩展数据库。
- 现有项目在验证阶段保持不动，继续承担线上流量。
- 新项目中的云对象需要 URL 化，客户端 App 通过 HTTP 调用。
- 集合名和业务字段保持不变，迁移不是业务数据模型重构。
- 从登录入口开始验证，当前对应接口为 `POST /api/users/oaid-login-v2`。
- 当前阶段只提交调研和方案文档，由你审核后再决定是否实现。

### 2.2 本轮未覆盖

- 未检查后台管理页面或 `pages` 目录。
- 未检查客户端 App 工程中的实际 request 封装、超时策略、重试策略和发版方式。
- 未连接线上服务空间，未确认线上实际部署版本是否与本地云函数源码完全一致。
- 未读取线上集合数量、数据量、索引、慢查询、实时读写量或账单明细。
- 未确认扩展数据库实例 ID、数据库名、MongoDB 版本、规格、剩余容量和备份策略。
- 未执行任何接口、数据迁移、并发、性能或回滚测试。

## 3. 当前云函数事实

### 3.1 总体盘点

对 `uniCloud-aliyun/cloudfunctions` 顶层 `package.json` 的只读盘点结果：

| 项目 | 当前结果 | 对迁移的影响 |
| --- | ---: | --- |
| 顶层云函数/云对象包 | 21 | 需要先确认哪些仍在生产使用，不能按目录名全部盲目迁移 |
| 已配置 URL 化路径 | 15 | App API、管理 API、测试入口和定时对象混在同一目录中 |
| 配置 Nodejs12 | 13 | 扩展数据库要求 Node.js 不低于 16，这些对象不能原样迁移 |
| 配置 Nodejs16 | 3 | 满足最低要求，但新项目建议统一验证 Nodejs18 |
| 配置 Nodejs18 | 1 | `legacy-migration-api` |
| 未显式配置 runtime | 4 | 必须在新项目明确指定并验证 |
| 已启用 `uni-cloud-ext-database` | 0 | 当前项目没有直接连接扩展数据库 |
| 带定时触发器 | 9 | 隔离测试期间必须关闭，避免重复任务 |

仓库名为 `uniCloud-aliyun`，但线上实际服务空间、云厂商和当前部署版本仍需在控制台核实，不能只依据目录名认定生产事实。

### 3.2 与客户端 App 直接相关的 URL 化对象

| 云对象 | 当前 URL 根路径 | runtime | 主要职责 | 首轮建议 |
| --- | --- | --- | --- | --- |
| `app-user-auth` | `/api/users` | Nodejs16 | OAID 登录/注册、用户信息、邀请、通知、购买记录；还带权益重置定时任务 | 第一条试点，但只在隔离库验证 |
| `app-system` | `/api/system` | Nodejs12 | 商品和 App 配置读取 | 登录后优先迁移的只读接口 |
| `app-photo-baidu` | `/api/yike` | Nodejs16 | 相册、文件、人物、漫画、播放地址等；还带定时任务 | 按“只读列表/详情”和“播放/权益写入”拆开迁移 |
| `app-bluetu-pay` | `/api/user/payment` | Nodejs12 | 下单、查单、支付通知、成功/取消跳转、支付状态同步 | 高风险，后期迁移 |
| `app-order-pay` | `/api/user/payment/v2` | Nodejs12 | 多支付平台下单和回调 | 高风险，确认是否仍在用后再迁移 |
| `legacy-migration-api` | `/api/legacy-migration` | Nodejs18 | 旧数据迁移查询接口 | 可作为项目间 URL 调用方式的参考，不属于登录主链路 |
| `photo-baidu` | `/api/v1/photobaidu` | Nodejs12 | 较早的内容接口 | 先确认客户端是否仍调用 |

管理、测试、自动更新类对象不应混入第一批 App API 切换；它们必须在生产调用关系确认后单独处理。

### 3.3 登录试点的真实数据流

当前 `app-user-auth/index.obj.js` 同时将以下两个方法映射到 V2 登录实现：

- `/api/users/oaid-login`
- `/api/users/oaid-login-v2`

建议以明确的新入口 `/api/users/oaid-login-v2` 作为试点，同时保留旧方法名兼容性测试，避免仍有旧版 App 调用 `/oaid-login`。

`verify-oaid-login-v2.js` 当前流程为：

1. 通过 `getHttpInfo()` 读取 URL 化 POST 请求体，并处理 Base64 请求体。
2. 解析 `device_oaid`、`app_platform`、`app_package_name`、`app_version`。
3. 校验包名和 OAID。
4. 按 `device_oaid` 查询 `user-accounts`。
5. 已有用户：检查封禁状态，更新登录 IP、登录时间和 VIP 状态，然后返回用户数据。
6. 新用户：读取全量邀请码，生成邀请码，写入邀请码集合，新增用户，再写入系统通知。

这条登录链路直接依赖至少三个集合：

- `user-accounts`
- `user-all-Invitation-codes`
- `system-app-notice`

其固定响应结构为：

```json
{
  "code": 200,
  "message": "...",
  "data": {}
}
```

当前业务码为成功 `200`、失败 `1005`、错误 `404`。首次迁移必须保持字段名、字段类型、业务码和主要提示语义兼容，不能顺便统一成另一个项目的 `code: 0/1` 契约。

### 3.4 默认 API 可继续使用，但不能依赖阿里云独有的 `affectedDocs`

扩展数据库支持现有 `uniCloud.database()` 默认数据库 API。新项目可以继续使用 `.collection()`、`.where()`、`.get()`、`.add()`、`.update()` 等方法；本方案不要求为了迁移而改用 `databaseForNative()`。

“API 方法兼容”不代表阿里云内置数据库额外附带的每个返回字段也存在。当前代码恰好依赖了官方单独列出的例外。

当前登录用以下判断识别已有用户：

```js
if (queryUserResult['affectedDocs'] == 1) {
  // 已有用户
} else {
  // 注册新用户
}
```

DCloud 官方开发指南在“阿里云 affectedDocs 属性处理”中明确说明：阿里云内置数据库会额外返回 `affectedDocs`，扩展数据库不返回该字段；查询、更新、删除应分别使用 `data.length`、`updated`、`deleted`。因此，如果原样复制当前判断，扩展数据库查询到已有用户后，条件仍为假，进而误走新用户注册分支。

这不是登录单点问题。`affectedDocs` 还出现在用户信息、邀请、通知、购买、商品、相册、人物、漫画、文件搜索、订单以及统计公共模块中。每迁移一个接口，都必须完成一次结果字段兼容审计，不能只修登录后假定其他接口兼容。

### 3.5 已确认的 P0 并发和重试风险

当前注册分支存在以下行为：

- 先读取一个保存全部邀请码的数组，在内存中生成新邀请码，再异步更新该数组。
- 邀请码集合更新没有等待完成。
- 新用户通知写入也没有等待完成。
- 按 OAID 查询与新增用户之间不是原子操作。

因此，客户端重复点击、网络超时重试、网关重试或并发登录可能造成：

- 同一个 OAID 重复注册。
- 两个用户生成相同邀请码。
- 用户已注册但邀请码总表或系统通知没有成功写入。
- 客户端超时后自动转回旧接口，在两套数据库各注册一次。

正式接入真实用户前，至少需要：

- 核实生产数据中 `device_oaid` 和 `my_invite_code` 是否已经存在重复值。
- 在无重复数据的前提下建立必要的唯一索引，或设计等价的原子占位方案。
- 为注册类请求增加幂等键或可重复执行语义。
- 禁止对“结果未知”的写请求自动切换数据库重试。
- 明确邀请码写入和系统通知失败时，是整体失败、补偿重试还是允许最终一致。

这些属于迁移前置安全修复，不能在未经审核时直接改线上旧代码；应先在新项目实现并测试，再决定是否回补旧项目。

### 3.6 DB Schema 不能替代接口校验

当前核心云函数通过 `uniCloud.database()` 使用传统 MongoDB API。DCloud 官方说明：传统 MongoDB API 不会应用 DB Schema 的权限、默认值和校验规则，只有 JQL 才能发挥 DB Schema 的这些能力。

因此，即使把 schema 文件复制到新项目，也不能假设现有云对象的入参、权限、默认值和字段类型会被 schema 自动保护。新 API 必须在服务端显式完成：

- HTTP 方法、Content-Type 和 Body 大小限制。
- JSON/Base64 解析错误处理。
- 字段必填、类型、长度、枚举和值域校验。
- 身份、资源归属和操作权限校验。
- 写入字段白名单，禁止客户端传入任意字段覆盖服务端状态。

### 3.7 定时任务和支付回调不能与旧项目同时启用

当前 `app-user-auth` 自带每周权益重置任务；内容更新类云对象还有多条周期任务。隔离测试项目如果直接带着触发器部署，可能在扩展数据库快照上提前改数据；正式切换时若新旧两边同时运行，则可能重复执行。

支付模块的通知地址和跳转地址还直接指向当前 URL。支付平台会重试异步通知，支付成功处理又会更新订单、用户金币或 VIP 权益。因此支付链路必须最后迁移，并满足：

- 新旧回调不会同时发放权益。
- 同一商户订单号重复通知只产生一次业务结果。
- 验签、订单金额、商户号、订单状态和用户归属全部复核。
- 切换第三方平台配置后能明确回滚。

## 4. `xdd-kefu-webscoket` 可借鉴的部分

参考项目已经采用以下扩展数据库工程方式：

- 需要数据库的云对象在 `package.json` 中启用 `uni-cloud-ext-database`。
- URL 化云对象普遍使用 Nodejs18。
- 通过 URL 根路径区分 `user`、`admin`、`shop`、`im` 等业务对象。
- 普通业务仍可使用 `uniCloud.database()`，集合名和常用操作集中到小型数据库模块；只有确需原生 MongoDB 能力时才使用原生对象。

本项目可复用的是“扩展库依赖 + Nodejs18 + URL 化对象 + 小型数据库访问层”的工程模式，不应复制参考项目的业务表、字段、响应码、鉴权逻辑或测试路径。

## 5. 推荐目标架构

### 5.1 隔离验证期

```text
线上 App ──当前 URL──> 现有项目云对象 ──> 内置数据库（继续承载生产）

测试 App/接口测试器 ──新 URL──> 新 uni 项目云对象 ──> 扩展数据库测试库（生产快照）
```

要求：

- 新项目使用单独的测试数据库名，不能让测试流量写入未来的正式库。
- 新项目所有定时触发器默认关闭。
- 支付平台回调地址保持旧地址，不允许指向测试项目。
- 只允许测试账号、测试设备和受控 IP/签名访问试点接口。
- 旧项目不增加代理、不改数据库依赖、不改变线上 URL。

### 5.2 正式切换后

```text
线上 App ──可配置的新 API Base URL──> 新 uni 项目云对象 ──> 扩展数据库正式库

现有项目 + 内置数据库 ──冻结保留──> 仅用于观察和受控回滚
```

客户端 App 应直接 HTTP 调用新项目的 URL 化云对象。除非客户端无法及时发版或无法配置 Base URL，否则不建议在旧项目新增“HTTP 代理云对象”再转发到新项目，因为代理会：

- 多一次网络和云函数调用。
- 增加超时、错误映射和日志排查层级。
- 继续消耗旧空间云函数资源。
- 让调用链和回滚边界更复杂。

如果确实需要旧项目代理，只能把它作为短期兼容网关，并单独审核超时、幂等、签名透传、客户端 IP、错误码和退役日期。

## 6. 分阶段实施方案

### 阶段 0：实施前确认

在写代码前确认：

1. 新 uni 项目所用云厂商和服务空间。
2. 已购扩展数据库的实例 ID、数据库版本、规格、容量和授权状态。
3. 测试库名、正式库名，二者必须物理隔离。
4. 客户端 App 工程位置，以及是否已有远程配置或环境化 API Base URL。
5. 是否允许正式切换时安排短暂停写窗口。
6. 当前实际在线 App 版本及其仍调用的旧接口清单。
7. 登录阶段是严格保持现有 OAID 逻辑，还是在正式放量前补充服务端令牌鉴权。

产物：决策记录、线上接口清单、集合/字段/索引清单、数据量清单、切换负责人和回滚负责人。

### 阶段 1：创建隔离新项目

1. 创建全新的 uni 项目和服务空间，不关联当前项目的内置数据库。
2. 授权新空间访问扩展数据库测试库。
3. HBuilderX 使用官方要求的受支持版本。
4. 新云对象统一使用 Nodejs18。
5. 每个使用数据库的云对象显式启用 `uni-cloud-ext-database`。
6. 设置独立测试 URL 根路径或测试域名，禁止覆盖现有生产 URL。
7. 默认不配置任何定时触发器和支付回调。
8. 增加仅报告版本、环境和数据库目标的健康检查；日志不得输出完整 OAID、支付签名、Cookie 或用户隐私数据。

阶段门禁：只有健康检查能明确证明“当前访问的是测试扩展库”，才允许导入数据和测试业务接口。

### 阶段 2：第一次数据迁移演练

官方给出的内置库到扩展库迁移方式是：在内置数据库中逐集合导出 JSON，再使用 MongoDB Compass 逐集合导入。官方同时说明内置数据库不支持一次批量导出全部集合。

演练步骤：

1. 从线上控制台导出集合、schema 和索引的独立清单。
2. 数据必须使用 JSON 格式，不使用 CSV，避免字段类型丢失。
3. 记录每个导出文件的集合名、导出开始/结束时间、文件大小和 SHA-256。
4. 在扩展数据库测试库创建同名集合。
5. 导入时保留原 `_id` 及其真实 BSON 类型，不重新生成 ID。
6. 先检查重复数据，再创建唯一索引；不要通过忽略冲突来“完成”导入。
7. 单独恢复 schema、索引和必要配置，因为单纯数据导入不会自动保留这些内容。
8. 对每个集合执行迁移核对。

每个集合至少核对：

- 总记录数相等。
- `_id` 去重后数量相等。
- 关键字段的类型分布相等，例如字符串、数字、日期、布尔、数组、ObjectId。
- 最早/最晚业务时间和空值数量相等。
- 用户、邀请、订单、购买、通知等引用字段不存在新增孤儿记录。
- 唯一键候选不存在重复，例如 OAID、邀请码、商户订单号。
- 随机样本和指定高风险样本逐字段一致。

第一次迁移只用于测试。导出完成后旧库仍会继续写入，所以该快照不能直接作为正式切换数据。

### 阶段 3：实现登录试点 API

新项目先只实现登录链路及其最小依赖，不复制整个云函数目录。

必须保持的外部契约：

| 项目 | 要求 |
| --- | --- |
| HTTP 方法 | `POST` |
| 路径 | `/api/users/oaid-login-v2`；旧 `/oaid-login` 是否兼容由在线版本清单决定 |
| Content-Type | `application/json`，并兼容 uniCloud URL 化可能产生的 Base64 Body |
| 请求字段 | 保持现有字段名和类型 |
| 响应字段 | `code`、`message`、`data` |
| 业务码 | 保持现有 `200/1005/404` 语义 |
| 用户数据 | 字段名、类型、空值含义和 OAID 脱敏方式保持兼容 |
| HTTP 状态 | 先记录当前线上实际行为，再决定是否保持；不能只比较 JSON Body |

实现前必须完成的兼容处理：

- 查询结果以 `data.length` 判断，不能依赖 `affectedDocs`。
- 更新和删除分别检查 `updated`、`deleted`。
- 验证扩展库上的 `updateAndReturn` 实际返回结构。
- 明确数据库异常、解析异常、超时和重复请求的响应。
- 为同一 OAID 并发注册建立唯一性与幂等保障。
- 邀请码分配不能依赖“读取全量数组后异步回写”的非原子流程。
- 系统通知写入失败的处理策略必须可观察、可补偿。

阶段门禁：登录所有自动化分支通过前，不允许客户端真实用户指向新 URL。

### 阶段 4：登录隔离验证和小流量验证

按以下顺序进行：

1. 在扩展数据库测试库使用脱敏快照或受控数据执行接口测试。
2. 对同一输入分别调用旧测试环境和新测试环境，比较外部响应与数据库副作用。
3. 不得对一次登录请求同时“影子调用”新旧生产接口，因为登录会写数据。
4. 使用专门测试构建或受控远程开关，让指定测试设备直连新 URL。
5. 观察请求量、成功率、P50/P95/P99 延迟、冷启动、数据库错误、重复注册和数据变化。
6. 任何异常都只关闭测试开关；现有线上流量不受影响。

客户端 request 层至少需要：

- Base URL 可配置并能按用户/设备白名单灰度。
- 明确连接超时和总超时。
- 读取请求可有限重试；写请求只在具备幂等键时重试。
- 对结果未知的登录/注册请求先查询确认，禁止无条件改投旧库。
- 日志携带 requestId，但不记录完整 OAID 和敏感 Body。

### 阶段 5：按风险迁移剩余 API

建议顺序：

1. 纯读取：`app-system` 的配置和商品读取。
2. 内容读取：相册、文件、人物、漫画、搜索、详情等确认无副作用的接口。
3. 用户状态写入：用户资料、通知已读、购买记录、邀请绑定。
4. 播放和权益：可能扣减次数、记录购买或生成缓存的接口。
5. 下单、查单、支付状态同步和第三方支付回调。
6. 定时任务、自动更新任务、管理接口和仍确认在用的遗留接口。

每个接口都必须独立完成：调用方确认、集合依赖、查询/写入副作用、`affectedDocs` 兼容、鉴权、幂等、契约对比、数据核对、灰度和回滚。不能因为登录成功就批量复制全部接口上线。

### 阶段 6：正式数据切换

截至本次查阅的官方迁移说明，官方给出的是逐集合导出 JSON 并导入 Compass 的方式，未给出内置数据库到扩展数据库的自动增量同步步骤。

对于当前以安全优先的项目，推荐短暂停写：

1. 提前完成至少一次全量迁移演练并记录耗时。
2. 发布前冻结会写库的 App 操作、支付下单、管理操作、回调消费和定时任务。
3. 等待在途请求结束，记录停写时间点和最后业务序号/时间。
4. 执行最终全量或经验证的差量迁移。
5. 重建并核对索引、schema、集合计数、关键字段类型和业务关联。
6. 对扩展库执行只读检查和受控写入检查。
7. 切换 App Base URL、第三方回调和定时任务，保证每种写入来源同一时刻只有一个生效端。
8. 先小流量，再逐步放量；所有门禁通过后解除停写。
9. 旧项目和旧库进入冻结保留状态，不立即删除。

如果业务不能接受短暂停写，就需要另外设计并验证双写、变更日志或增量同步。该方案复杂度和数据风险明显更高，不应在本方案中默认启用，必须单独立项审核。

### 阶段 7：稳定观察和退役

- 连续核对新库记录量、核心业务指标、支付权益、邀请关系和异常日志。
- 通过账单确认内置数据库读写费用确实下降，而不是只把少量接口迁走。
- 在保留期内禁止旧端写入，但保留只读核查和受控回滚能力。
- 保留期、回滚数据处理方式和最终删除时间由你确认，本方案不预设天数。
- 未经单独审批，不删除旧项目、旧云函数、旧数据库或备份。

## 7. 回滚方案

### 7.1 隔离测试阶段

关闭测试 App 的新 Base URL 开关即可。线上 App、旧项目和旧库没有改动，风险最低。

### 7.2 正式切换但扩展库尚未接收新写入

停止新入口，恢复旧 URL，检查旧库仍是最新权威数据后恢复服务。

### 7.3 扩展库已经接收新写入

不能直接恢复旧 URL。正确顺序是：

1. 再次停止所有写入。
2. 确认扩展库切换后新增和修改的数据范围。
3. 将新库增量安全回迁旧库，并处理冲突。
4. 核对用户、订单、权益、邀请和通知。
5. 确认旧库重新成为最新权威数据后，才能切回旧 URL。

支付订单和权益变更必须逐单核对，不能用简单覆盖处理。若没有可验证的反向增量方案，正式放量门禁不得通过。

## 8. 安全要求

URL 化云对象是公网 HTTP 入口。DCloud 官方明确要求开发者自行实现权限控制和安全防护；恶意调用还会消耗云函数资源。

当前登录校验的包名、平台和 OAID 均由客户端传入。包名白名单只能作为输入校验，不能证明请求来自可信客户端。正式放量前应明确两种方案之一：

1. **严格兼容方案**：首期保持 OAID 登录行为不变，只通过测试白名单、限流、风控和幂等降低风险；后续单独升级鉴权。
2. **先补鉴权方案**：登录成功后签发服务端可验证的短期访问令牌，其余用户接口验证令牌和资源归属；这会改变客户端与 API 契约，必须单独审核。

无论选择哪一种，都必须具备：

- HTTPS。
- 路由级 HTTP 方法白名单。
- 请求体大小和字段长度限制。
- 频率限制、异常 OAID/设备/IP 风控。
- 写请求 requestId/幂等键和重放保护。
- 用户只能操作自己的数据；不能只信任 Body 中的 `user_id`。
- 支付回调强制验签并核对订单金额和商户信息。
- 日志脱敏、密钥不入库不入日志。
- 测试接口、管理接口和生产 App API 使用不同入口和访问策略。

## 9. 自动化测试与验收矩阵

当前全部状态均为“未执行（方案阶段）”。开始实施后必须逐项补充具体测试文件、命令和执行结果。

### 9.1 登录/API 矩阵

| 能力或分支 | 测试层级 | 具体检查 | 当前状态 |
| --- | --- | --- | --- |
| 合法 JSON 登录 | API 集成测试 | 响应契约、用户字段、登录时间/IP/VIP 副作用 | 未执行 |
| Base64 Body | API 集成测试 | 解码、解析和响应与普通 JSON 一致 | 未执行 |
| 非法 JSON/空 Body | API 集成测试 | 不崩溃、不写库、错误契约稳定 | 未执行 |
| 包名不在白名单 | 单元 + API | 拒绝且不查询/写入用户业务数据 | 未执行 |
| OAID 缺失、空白、超长、类型错误 | 单元 + API | 拒绝且不写库 | 未执行 |
| 已有正常用户 | API + 数据核对 | 不误注册，更新字段正确 | 未执行 |
| 已封禁用户 | API + 数据核对 | 拒绝且不改权益 | 未执行 |
| VIP 有效/过期/字段缺失 | 数据驱动单元 + API | 结果和旧接口一致 | 未执行 |
| 新用户注册 | API + 数据核对 | 用户、邀请码、通知结果符合约定 | 未执行 |
| 同一 OAID 并发注册 | 并发集成测试 | 最终只有一个用户 | 未执行 |
| 邀请码并发分配 | 并发集成测试 | 无重复、无丢失 | 未执行 |
| 请求超时后重试 | 故障注入 | 不重复注册，不跨库重复写 | 未执行 |
| 数据库查询/更新/新增失败 | 故障注入 | 错误可观察，无部分静默成功 | 未执行 |
| 系统通知写入失败 | 故障注入 | 按确认策略回滚或可补偿 | 未执行 |
| 旧 `/oaid-login` 路径 | 契约测试 | 按在线版本决策保留或明确下线 | 未执行 |
| 旧版 `app_version` | 数据驱动 API | 提示语义与当前接口一致 | 未执行 |
| HTTP 方法/Content-Type 错误 | API 安全测试 | 明确拒绝，不执行方法 | 未执行 |
| 超大 Body、恶意频率 | 安全/容量测试 | 限制生效，资源消耗可控 | 未执行 |

### 9.2 数据迁移矩阵

| 能力或分支 | 测试层级 | 具体检查 | 当前状态 |
| --- | --- | --- | --- |
| 全集合覆盖 | 清单契约测试 | 源集合与迁移清单集合相等 | 未执行 |
| 记录总数 | 数据对账 | 每集合源/目标计数相等 | 未执行 |
| `_id` 和 BSON 类型 | 数据对账 | 值与类型都保持一致 | 未执行 |
| 字段/类型分布 | 数据对账 | 未新增类型漂移或字段丢失 | 未执行 |
| 唯一键 | 数据质量测试 | OAID、邀请码、订单号重复清单为零或已处置 | 未执行 |
| 关联完整性 | 数据质量测试 | 用户—邀请—订单—权益—通知无新增孤儿 | 未执行 |
| 索引 | 结构契约测试 | 源/目标索引集合和唯一性一致 | 未执行 |
| schema | 结构契约测试 | 文件/线上配置与目标一致 | 未执行 |
| 大集合导入 | 迁移演练 | 可在窗口内完成，失败可续传/重跑 | 未执行 |
| 最终同步 | 切换演练 | 停写点之后源库无新增写入 | 未执行 |

### 9.3 支付、定时和回滚矩阵

| 能力或分支 | 测试层级 | 具体检查 | 当前状态 |
| --- | --- | --- | --- |
| 重复支付通知 | API 集成测试 | 只发放一次权益 | 未执行 |
| 乱序/延迟通知 | API 集成测试 | 状态不倒退、不重复发放 | 未执行 |
| 验签失败/金额不符 | 安全测试 | 拒绝且不写权益 | 未执行 |
| 新旧回调切换 | 切换演练 | 同一时刻仅一个消费者生效 | 未执行 |
| 定时任务启停 | 调度集成测试 | 同一任务同一时刻只运行一份 | 未执行 |
| 切换前回滚 | 演练 | 可恢复旧 URL 且无数据差异 | 未执行 |
| 切换后反向同步 | 演练 | 新库增量可完整回迁并核对 | 未执行 |
| 账单效果 | 运行观测 | 内置库读写量与费用按预期下降 | 未执行 |

## 10. 上线门禁

任一条件不满足，不得进入正式流量：

- 已确认线上实际调用的 API、定时任务和支付回调完整清单。
- 扩展数据库实例、库名和环境隔离可被日志明确识别。
- 所有迁移集合的数据、字段类型、索引和关联核对通过。
- 当前接口中的 `affectedDocs` 依赖已按方法语义逐处处理并测试。
- 登录并发注册、邀请码唯一性和超时重试测试通过。
- 新旧接口契约测试通过，客户端可控灰度和回退已验证。
- 新项目测试期间没有启用生产定时任务和支付回调。
- 最终同步、停写、切换和切换后回滚至少完整演练一次。
- 监控、告警、requestId 和日志脱敏已验证。
- 关键自动化测试全部执行且通过；未自动化项有明确技术原因、步骤和剩余风险。

## 11. 成本预期

扩展数据库官方说明读写次数不再按内置数据库方式计费，但这不等于整个云空间零费用。URL 化请求、云函数运行、流量、存储和其他云资源仍可能产生费用。

只迁移登录接口通常不会立刻消除每天的大部分数据库读写费用。要达到你的成本目标，最终需要把实际产生高读写量的 App API、定时任务、支付处理和其他后台写入全部切到扩展数据库，并用切换前后的账单和调用量验证效果。

## 12. 待你审核的关键决策

1. 是否同意“新项目完全隔离，客户端 App 最终直接 HTTP 调新 URL，不在旧项目长期做代理”。
2. 是否同意首个试点固定为 `POST /api/users/oaid-login-v2`，先在测试扩展库验证，绝不直接接真实用户。
3. 是否接受正式切换时安排短暂停写窗口；这是当前最稳妥的数据一致性方案。
4. 登录首期选择“保持现有 OAID 契约”还是“先补服务端令牌鉴权”。
5. 是否同意支付回调和定时任务放在最后一批迁移。
6. 下一阶段是否先只创建新项目骨架、数据迁移清单和登录 API 的测试设计，仍不接触现有线上项目。

## 13. 官方资料

- [uni 扩展数据库介绍](https://doc.dcloud.net.cn/uniCloud/ext-mongodb/intro.html)
- [扩展数据库开发指南、启用条件、`affectedDocs` 差异和官方迁移步骤](https://doc.dcloud.net.cn/uniCloud/ext-mongodb/dev.html)
- [云函数/云对象 URL 化](https://doc.dcloud.net.cn/uniCloud/http)
- [云对象的 `getHttpInfo()` 说明](https://doc.dcloud.net.cn/uniCloud/cloud-obj.html)
- [DB Schema 概述及传统 MongoDB API 不生效的说明](https://doc.dcloud.net.cn/uniCloud/schema)
- [数据库备份、导入导出和 JSONL 注意事项](https://doc.dcloud.net.cn/uniCloud/hellodb.html)
- [扩展数据库费用和实例规格说明](https://doc.dcloud.net.cn/uniCloud/ext-mongodb/price.html)
