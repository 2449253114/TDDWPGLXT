下面是我的代码，如何需求也在代码中写上了，你帮我看看要怎么取出我需要的数据，
我目前有在使用cheerio框架，你看看用cheerio要怎么实现，如果实现不了，可以用普通的js代码去帮我实现
// 当前URL，减1是因为索引从0开始
const BASE_URL = this.task.urlsList[this.task.page.current - 1].url
return new Promise((resolve, reject) => {
	http({
		url: `${BASE_URL}`,
		method: 'GET'
	}).then(async res => {
		//console.log(res)
		if (!res.data) {
			uni.showToast({
				title: '请求失败'
			})
			reject(new Error('Request Error'));; // 如果不存在，直接返回
		}
		
		const html2 = res.data
		console.log('html2', html2)
		
		`html2打印如下
		<!DOCTYPE html>
		<html class="ks-webkit537 ks-webkit ks-chrome33 ks-chrome">
		<head>
			<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
			<title>Phoenix</title>
			<!--不需要更改标题，只需要更改shareTitle中值即可-->
			<meta charset="utf-8">
			<link rel="stylesheet" href="bmwe/index.css">
			<meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0,user-scalable=no">
			<meta http-equiv="Cache-Control" content="max-age=0">
			<meta name="apple-touch-fullscreen" content="yes">
			<meta name="apple-mobile-web-app-capable" content="yes">
			<meta name="apple-mobile-web-app-status-bar-style" content="black">
			<meta name="apple-mobile-web-app-capable" content="yes">
			<meta name="apple-mobile-web-app-status-bar-style" content="black">
		
		
		
		<script>
				//---------更改项，其他无特殊情况不需要更改---------
				var shareTitle = '肉体劳动者';
				var pageNum = 9; //图片页数
				//--------------------------------------------
			
			
				//获取页面url地址，例如http://www.phoenixbear.tk/20141206/index.html
				var currentPageUrl = "http://www.ubearcn.com/bc/20200508w/index.html";
				if (typeof this.href === "undefined") {
			 	   currentPageUrl = document.location.toString();
				}
				else {
					currentPageUrl = this.href.toString();
				}
				//获取页面当前url路径，例如http://www.phoenixbear.tk/20141206/
				var docUrl = currentPageUrl.substring(0,currentPageUrl.lastIndexOf('/',currentPageUrl.length-1)) + '/';
		        //var imgUrl = 'http://www.phoenixbear.tk/bc/20141206/pic.jpg';
				imgUrl = docUrl + 'pic.jpg';  //这段代码需要测试一下，如果测试不通过就用上面原来的代码
		        //var lineLink = 'http://www.phoenixbear.tk/bc/20141206/index.html';
				lineLink = docUrl + 'index.html';	//这段代码需要测试一下，如果测试不通过就用上面原来的代码
				
				
		        var descContent = shareTitle + '.建议连入wifi阅读.Made by Phoenix';
		        var appid = '';
				var dir = 'bmwe' //图片目录，相对路径
				
				document.title= shareTitle; //设置title显示
				
		        function shareFriend() {
		            WeixinJSBridge.invoke('sendAppMessage',{
		                "appid": appid,
		                "img_url": imgUrl,
		                "img_width": "200",
		                "img_height": "200",
		                "link": lineLink,
		                "desc": descContent,
		                "title": shareTitle
		            }, function(res) {
		                //_report('send_msg', res.err_msg);
		            })
		        }
		        function shareTimeline() {
		            WeixinJSBridge.invoke('shareTimeline',{
		                "img_url": imgUrl,
		                "img_width": "200",
		                "img_height": "200",
		                "link": lineLink,
		                "desc": descContent,
		                "title": shareTitle
		            }, function(res) {
		                   //_report('timeline', res.err_msg);
		            });
		        }
		        function shareWeibo() {
		            WeixinJSBridge.invoke('shareWeibo',{
		                "content": descContent,
		                "url": lineLink,
		            }, function(res) {
		                //_report('weibo', res.err_msg);
		            });
		        }
		        // 当微信内置浏览器完成内部初始化后会触发WeixinJSBridgeReady事件。
		        document.addEventListener('WeixinJSBridgeReady', function onBridgeReady() {
		            // 发送给好友
		            WeixinJSBridge.on('menu:share:appmessage', function(argv){
		                shareFriend();
		            });
		            // 分享到朋友圈
		            WeixinJSBridge.on('menu:share:timeline', function(argv){
		                shareTimeline();
		            });
		            // 分享到微博
		            WeixinJSBridge.on('menu:share:weibo', function(argv){
		                shareWeibo();
		            });
		        }, false);
		        </script>
		
		
		
		
		</head>
		<body>
			<section class="p-index" style="height: 589px; display: block;">
				<script>
				
				var picName = "";
				for(var i = 1; i <= pageNum; i++){
					if (i == 1)
				    	document.write("<section class=\"m-page m-page1 show\">");  
					else 
						document.write("<section class=\"m-page m-page2 hide\">");
					
					if (i < 10)
						picName = '0'+ i + '.jpg';
					else
						picName = i + '.jpg';
					
						document.write("<div class=\"m-img m-img01\" style=\"background: url(" + dir + "\/" + picName + ") 0 0 no-repeat;background-size:cover;\"></div>");
						document.write("</section>\n");
						
				  }
				  
				</script>
		
		
			</section>
			<script src="bmwe/plug.preload.js"></script>
		    <script src="bmwe/jquery-1.8.2.min.js"></script>
		    <script src="bmwe/jquery.easing.1.3.js"></script>
		    <script src="bmwe/99_main_1.js"></script>
		</body>
		</html>
		`
我需要取出来的数据是script中的var shareTitle = '肉体劳动者';（也就是拿到字符串"肉体劳动者"）,然后是var pageNum = 9; //图片页数（也就是拿到图片数量"9"）, 然后是var currentPageUrl = "http://www.ubearcn.com/bc/20200508w/index.html";（也就是拿到网址），需要用中文回答。
