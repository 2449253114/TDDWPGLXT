注册违规检查系统
1、页面上有输入几天内相同IP地址注册（用于查询从比如按今天现在时间往前推几天的注册用户，但往前推几天的起始时间是比如3天内，那就是今天不算，因为今天只过了一半，往前推的算法是从今天0点开始推3天且是3天前的0点0分0秒，比如今天是5月19日，往前推3天是从昨天开始算，起始时间就是5月16日0点0分0秒）

2、页面上有按日历、按今天（现在）、昨天、前天、几天前的方式去选择截止日。

3、将用户用uni-table表格渲染（也有风险等级等，可参考邀请违规的用户表格）

4、统计同一IP地址注册的用户数量，和相同IP的邀请人邀请码。

（这个需求是用于快速查询违规注册账号，也可用于同IP注册账号不足50人，但也是大量注册，或者）




# 封禁高风险IP相关的注册用户和注册用户的邀请人账号，且把高风险IP加入黑名单（IP黑名单可先不在注册登录接口中拦截，但要每天人工查询下相同IP注册数量）


# 永久会员查询，有新增用户则放到最前面


# （已知隐患未除）即使把高风险永久会员封禁，该用户还是能白嫖，比如手机有50个分身，那么到了免费观看日期，免费日每天可以看50次，这是不行的，所以需要把高风险永久会员的所有邀请用户的数据都更改，使用.set方法（db.collection(collectionName).doc(docId).set）来重写该条记录。（set方法说明：覆写记录;会删除操作的记录中的所有字段，创建传入的字段。如果操作的记录不存在，会自动创建新的记录），将用户信息字段仅保留_id、用户自身邀请码、邀请人id、邀请时间、设备号、最后登录时间、最后登录ip、注册时间、注册ip，其他的字段全部不要，这样做之后登录App是登录不上的，因为字段不全会导致应用闪退，这样就有效解决了该用户主账号（邀请人）永久会员被封禁之后通过分身应用的账号（被邀请者）白嫖免费观看




邀请违规：
在永久会员区域增加一个分区，把新增用户单独这个分区显示，且需要手动让新增用户添加到已处理。（就是说有新增未处理分区，有已处理分区，大概是这种意思，作用就是防止误操作，比如新增5个用户，突然刷新了网页，从本地读取后在和接口查询对比，没有新增了，但新增用户都没手动检查比如查看他邀请的用户的IP和注册时间等以便人工分析到底这个用户是不是有违规邀请的嫌疑等）



结合极光短信或个推短信服务来使用手机号短信验证码登录，且手机号和设备号绑定，且一个手机号只能一个设备号，哪怕分身，由于设备号不一致，将无法登录。



邀请奖励改为人工审核通过后发放奖励，且以自行领取使用的方式，不再直接自动充值，而是用户自己点击使用（这样做的好处可以人工检查确认一次是否是违规邀请，违规邀请不予发放。）




# 需求
1、找出最近指定天数（输入框中）内注册相同IP或登录IP相同的账号，比如A注册IP=A注册IP，或A注册IP=A登录IP，或A注册IP=B登录IP，或A登录IP=B注册IP（A和B代表不同账号）
2、对这些账号的注册IP和登录IP加入IP黑名单，禁止再注册与登录
3、对这些账号和这些账号绑定的上级邀请人账号全部封禁，且把邀请人的注册IP和登录IP也加入黑名单。
4、相同IP的账号要查询账号邀请人的邀请码，邀请人邀请码一样，不一样，无邀请人的。（将相同IP账号的邀请人id都记录，最后在查询邀请人id信息之前，把相同IP的邀请人id拿过来去查询，咋说了，我意思是，假设50个人的IP都是一样，并且这50个人的邀请人id都是同一个，查询邀请人信息时，拿到的也就是一个邀请人id，我意思不要一个一个的去查邀请人信息，因为50个IP相同的人都是同一个邀请人id，就没必要查50次同一个邀请人信息，懂了吗？）









帮我实现应用过滤条件

```vue
<!-- 相同IP分组数据 -->
<view v-if="violationGroups.length > 0">
	<uni-collapse>
		<uni-collapse-item
			v-for="(group, groupIndex) in violationGroups" 
			:key="groupIndex"
			:title="'IP: ' + group.ip + ' (用户数: ' + group.users.length + ', 风险等级: ' + group.ipRiskLevel + ')'"
			:open="groupIndex === 0">

			<uni-card
				:is-shadow="false" 
				:is-full="false" 
				:border="true" 
				margin="0 0 15px 0">
				
				<view class="ip-group-header">
					<view class="ip-info">
						<text class="ip-address">IP: {{ group.ip }}</text>
						<text class="risk-level" :class="getRiskLevelClass(group.ipRiskLevel)">
							风险等级: {{ group.ipRiskLevel }}
						</text>
						<text class="user-count">注册用户数: {{ group.users.length }}</text>
					</view>
					<view class="inviter-info">
						<text class="inviter-label">邀请人信息:</text>
						<view v-if="group.inviterInfo.length > 0">
							<view 
								v-for="(inviter, inviterIndex) in group.inviterInfo" 
								:key="inviterIndex"
								class="inviter-item">
								<text class="inviter-code">邀请码: {{ inviter.inviteCode || '无' }}</text>
								<text class="inviter-count">邀请数量: {{ inviter.count }}</text>
								<text class="risk-level" :class="getRiskLevelClass(inviter.riskLevel)">
									风险等级: {{ inviter.riskLevel }}
								</text>
								<button 
									v-if="inviter.inviteCode && inviter.inviteCode !== '无'" 
									class="ban-inviter-btn" 
									size="mini" 
									type="warn"
									:disabled="inviter.status === 3"
									@click="banInviter(inviter.inviteCode, inviter.inviterId)">
									{{ inviter.status === 3 ? '已封禁' : '封禁邀请人' }}
								</button>
								<button 
									v-if="inviter.inviteCode && inviter.inviteCode !== '无'" 
									size="mini" 
									type="primary"
									@click="editUser(inviter.inviterId)">
									修改
								</button>
								<button 
									v-if="inviter.inviteCode && inviter.inviteCode !== '无'" 
									size="mini" 
									type="default"
									@click="checkViolationInvitation(inviter.inviteCode)">
									检测违规邀请
								</button>
							</view>
						</view>
						<text v-else class="no-inviter">无邀请人</text>
					</view>
				</view>

				<!-- 用户列表表格 -->
				<uni-table ref="table" :loading="false" border stripe>
					<uni-tr>
						<uni-th width="80" align="center">用户状态</uni-th>
						<uni-th width="100" align="center">邀请码</uni-th>
						<uni-th width="140" align="center">受邀时间</uni-th>
						<uni-th width="140" align="center">最后登录</uni-th>
						<uni-th width="120" align="center">最后登录IP</uni-th>
						<uni-th width="140" align="center" sortable @sort-change="sortChange($event, 'register_date')">注册时间</uni-th>
						<uni-th width="120" align="center">注册IP</uni-th>
						<uni-th width="140" align="center">操作</uni-th>
					</uni-tr>
					<uni-tr v-for="(user, userIndex) in group.users" :key="userIndex">
						<uni-td align="center">
							<text :class="getStatusClass(user.status)">
								{{ getStatusText(user.status) }}
							</text>
						</uni-td>
						<uni-td align="center">{{ user.inviterInviteCode || '无' }}</uni-td>
						<uni-td align="center">
							{{ user.invite_time ? formatTimestampToDateTime(user.invite_time) : '无' }}
						</uni-td>
						<uni-td align="center">
							{{ formatTimestampToDateTime(user.login_date) }}
						</uni-td>
						<uni-td align="center">{{ user.login_ip }}</uni-td>
						<uni-td align="center">
							{{ formatTimestampToDateTime(user.register_date) }}
						</uni-td>
						<uni-td align="center">{{ user.register_ip }}</uni-td>
						<uni-td align="center">
							<view class="action-buttons">
								<button 
									size="mini" 
									type="warn" 
									@click="banUser(user._id, userIndex, groupIndex)"
									:disabled="user.status === 3">
									{{ user.status === 3 ? '已封禁' : '封禁' }}
								</button>
								<button 
									size="mini" 
									type="primary" 
									@click="editUser(user._id)">
									修改
								</button>
							</view>
						</uni-td>
					</uni-tr>
				</uni-table>
			</uni-card>
		</uni-collapse-item>
	</uni-collapse>
</view>

/**
 * 表格排序条件变化处理
 * @param {Object} e - 事件对象
 * @param {String} field - 字段名
 */
sortChange(e, field) {
	this.sortOptions.field = field
	this.sortOptions.order = e.order
	this.applyFilters()
},

/**
 * 应用过滤和排序条件
 */
applyFilters() {
	
},
```

注意，sortChange是表头中的功能，那么用户排序（正序、倒序）过滤当然是对当前的IP组里的数据排序，懂了吗？
然后我需要在处理违规分组数据时，也需要按照用户注册时间倒序，最近的时间靠前，最久远的时间靠后。
