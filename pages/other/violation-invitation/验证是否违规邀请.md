我现在有个需求是这样的（我这是uniapp项目）：验证是否违规邀请

页面大致是这样的（可参考下方的kefu-activate-vip.vue文件代码）：
说明卡片（就是页面的作用）
账户邀请码：请输入用户自身账号邀请码、xx按钮（按钮名字你帮我起名，作用就是查询此账号邀请的人是否有归为行为，具体违规行为下面会为你解释）
邀请记录：表头（设备号、邀请码、邀请时间、最后登录IP）


kefu-activate-vip.vue

```vue
<template>
	<view class="container">
		<uni-card class="card" :is-shadow="false" :is-full="false" :border="true" margin="0 0 10px 0">
			<text class="uni-card-txt">人工充值（客服帮激活VIP）</text>
		</uni-card>

		<uni-forms-item label="账号邀请码" :label-width="85">
			<div class="custom-forms-item">
				<uni-easyinput v-model="myInviteCode" :maxlength="6" placeholder="请输入用户自身账号邀请码" @clear="clearData" />
				<div class="space-10" />
				<button type="primary" size="mini" @click="queryUser">查询用户</button>
			</div>
		</uni-forms-item>
		
		<div v-if="userId != ''">
			<uni-forms-item label="下单用户ID" :label-width="85">
				<div class="custom-forms-item">
					<uni-easyinput disabled placeholder="下单用户ID" v-model="userId"></uni-easyinput>
					<div class="space-10" />
					<button class="button" size="mini" type="primary" @click="editUser(userId)">查看详情</button>
				</div>
			</uni-forms-item>
		</div>

		<div v-if="userExists">
			<!-- 选择开通会员的时长 -->
			<uni-forms-item>
				<div class="vip-buttons">
					<div v-for="(option, index) in goods" :key="index"
						:class="['vip-button', { selected: selectedOption === index }]" @click="selectOption(index)">
						<p><span class="price">{{ option.price }}</span>元</p>
						<p style="color: #757575">{{ option.name }}</p>
					</div>
				</div>
			</uni-forms-item>
			<uni-forms-item label="订单号" :label-width="85">
				<uni-easyinput v-model="payNo" placeholder="请输入订单号" />
			</uni-forms-item>
			<uni-forms-item label="支付渠道" :label-width="85">
				<uni-data-checkbox v-model="payType" :localdata="payTypeData"></uni-data-checkbox>
			</uni-forms-item>
			<button type="primary" @click="submit()">提交</button>
		</div>
	</view>
</template>

<script>
	const db = uniCloud.database();
	const dbCmd = db.command;

	// 用户数据库表
	const userCollectionName = 'user-accounts'
	const userCollection = db.collection(userCollectionName)

	// 用户VIP会员更变数据库表
	const userVipCollectionName = 'user-vip-changes'
	const userVipCollection = db.collection(userVipCollectionName)

	// 支付订单数据库表
	const payOrdersCollectionName = 'user-payment-orders'
	const payOrdersCollection = db.collection(payOrdersCollectionName)

	// 商品列表
	const shopGoodsCollectionName = 'shop-goods'
	const shopGoodsCollection = db.collection(shopGoodsCollectionName)

	// APP系统通知数据库表
	const appSystemNoticeCollectionName = 'system-app-notice'
	const appSystemNoticeCollection = db.collection(appSystemNoticeCollectionName)

	export default {
		data() {
			return {
				myInviteCode: '', // 用户的账号自身邀请码
				userExists: false, // 用户是否存在
				userId: '', // 用户ID
				userInfo: {}, // 用户信息
				goods: [{ // 开通会员的套餐
					price: '135',
					name: '1个月',
					type: 1, // 商品类型：0 金币充值 、1 会员开通
					day_count: 0, // 开通天数
					giveaway_coin: 0, // 赠送金币
				}],
				selectedOption: null, // 选择开通会员的时长
				payNo: '', // 订单号
				payType: 'alipay', // 支付渠道，默认支付宝-转账码
				payTypeData: [{
					text: '支付宝',
					value: 'alipay'
				}, {
					text: '微信',
					value: 'wxpay'
				}]
			};
		},
		onShow() {
			const timestampInSeconds = Math.floor(Date.now() / 1000);
			const timestampInMilliseconds = Date.now();
			
			const paySuccessTimeFromSeconds = this.formatTimestampToDateTime(timestampInSeconds);
			const paySuccessTimeFromMilliseconds = this.formatTimestampToDateTime(timestampInMilliseconds);
			
			console.log(paySuccessTimeFromSeconds); // 输出格式为 "2024-05-19 00:16:05"
			console.log(paySuccessTimeFromMilliseconds); // 输出格式为 "2024-05-19 00:16:05"
		},
		methods: {
			selectOption(index) {
				this.selectedOption = index;
			},
			clearData() {
				this.userExists = false
				this.myInviteCode = ''
				//this.userId = '' // 这个先不清空，用于在充值成功后，保留显示用户ID和查看用户信息详情页
				this.userInfo = {}
				this.selectedOption = null
				this.payNo = ''
				this.payType = 'alipay'
			},
			/**
			 * 编辑用户数据
			 * @param { String } id
			 */
			editUser(userId) {
				uni.navigateTo({
					url: `/pages/member/users/edit?id=${userId}`
				})
			},
			async queryUser() {
				// 查询用户
				if (this.myInviteCode == '' || this.myInviteCode.length != 6) {
					uni.showToast({
						title: '无效邀请码',
						icon: 'none'
					})
					return
				}

				uni.showLoading({
					mask: true
				})
				// 使用嵌套解构赋值获取data属性
				const {
					result: {
						data
					}
				} = await userCollection.where({
						my_invite_code: this.myInviteCode
					})
					.get();

				if (data.length == 0) {
					uni.showToast({
						title: '用户不存在',
						icon: 'none'
					})
					return
				}

				//console.log('data[0]', data[0])
				await this.queryGoods()

				this.userInfo = data[0]
				this.userId = data[0]._id
				this.userExists = true

				uni.hideLoading()
			},
			async queryGoods() {
				const {
					result: {
						data
					}
				} = await shopGoodsCollection.where({
					type: 1
				}).get() // 会员开通商品
				this.goods = data
			},
			// 提交并开通会员
			async submit() {
				if (this.userExists == false) return
				if (this.selectedOption == null || this.payNo === '' || this.payNo.length < 10) {
					console.log('请确保已选择一个选项，并且订单号不少于10个字符。');
					// alert('请确保已选择一个选项，并且订单号不少于10个字符。');
					uni.showToast({
						title: '参数效验失败',
						icon: 'none'
					})
					return;
				}

				uni.showLoading({
					mask: true
				})

				// 更新用户VIP时长
				await this.updateUserVipExpireDate(this.userId)
				
				// 发送系统通知
				await this.sendSystemNotification(this.userId);
				
				// 添加VIP变更记录
				await this.addVipChangeRecord(this.userId);
				
				// 清除必要数据
				this.clearData()
				
				uni.hideLoading()
				uni.showToast({
					title: '充值成功'
				})
			},

			// 更新用户VIP过期日期的逻辑
			async updateUserVipExpireDate(userId) {
				// 按天数计算时间戳
				const day_count = this.goods[this.selectedOption].day_count // 天数（要开通几天）
				const day_count_timestamp = day_count * 24 * 60 * 60 * 1000 // 天数时间戳（单位：毫秒）

				// 条件：(计算会员有效期至)
				// 1. 如果到期时间不为空且小于当前时间，则从当前时间开始加上续费周期时间。
				// 2. 如果到期时间不为空且大于当前时间，则从到期时间开始加上续费周期时间。

				// 新的过期时间
				let newExpireDate;
				// 获取当前时间的时间戳（毫秒）
				const now = Date.now();

				// 获取用户会员到期时间
				const userVipExpireDate = this.userInfo.vip_expire_date

				// 判断用户会员到期时间是否小于当前时间
				if (userVipExpireDate < now) {
					// 如果小于当前时间，则从当前时间开始加上续费周期时间
					newExpireDate = now + day_count_timestamp
				} else {
					// 如果大于当前时间，则从到期时间开始加上续费周期时间
					newExpireDate = userVipExpireDate + day_count_timestamp
				}

				//console.log('newExpireDate', newExpireDate)

				const {
					result: {
						updated
					}
				} = await userCollection
					.doc(userId)
					.update({
						vip: true, // 是否会员
						vip_expire_date: newExpireDate
					})

				// updated	Number	更新成功条数，数据更新前后没变化时会返回0
				console.log(userId, updated)

			},
			// 发送系统通知给用户的逻辑
			async sendSystemNotification(userId) {
				await appSystemNoticeCollection.add({
					user_id: userId,
					title: '会员充值通知',
					content: '亲爱的用户，您的会员充值已成功。现在您可以享受所有VIP特权。感谢您的支持，祝您使用愉快。',
					is_read: false, // 通知未读
					create_date: Date.now() // 添加创建时间
				});
			},
			// 添加VIP订单记录的逻辑
			async addVipChangeRecord(userId) {
				const goodName = `人工充值会员（${this.goods[this.selectedOption].name}）`
				const goodPrice = this.goods[this.selectedOption].price
				const dayCount = this.goods[this.selectedOption].price
				// 当前时间的时间戳：如果你需要一个10位的时间戳（代表以秒为单位的时间戳，而不是以毫秒为单位），你可以将Date.now()的结果除以1000，然后使用Math.floor来舍去小数点后的部分。
				const timestamp = Math.floor(Date.now() / 1000);
				
				// 默认数据
				const otherParams = {
					body: goodName, // 商品名称
					pay_type: this.payType, // 支付渠道，wxpay | alipay
					out_trade_no: `GOOD${this.payNo}`,// 自己系统订单号，也叫商户自己生成的订单号
					total_fee: goodPrice,// 订单总金额
					timestamp, // 发起订单时间（秒）
					order_type: 1, // 订单类型 0：金币充值 1：会员开通
					status: 1, // 订单状态 0：未支付 1：已支付 2：已退款
					day_count: dayCount, // 开通会员天数（仅用于充值会员时的会员天数）
					system_recharge_issuccess: true, // 系统充值是否成功（用来记录当给用户充值金币/会员完成时记录true，防止同订单重复充值，带来的bug）
					is_payment_status_checked: true, // 支付状态检查标记（此字段用于标识订单的支付状态是否已经在第三方支付平台进行了查询。当用户完成支付操作后，如果由于应用被重启或其他原因导致本地支付状态未及时更新，这个字段会在重启后用来检查并同步第三方支付平台的支付状态。true 表示已检查并确认了第三方支付平台的支付状态，无论是支付完成还是未完成；false 表示本地数据库状态尚未与第三方支付平台同步。这确保了即使在应用重启后，用户的支付状态也能被准确追踪和处理，以便及时更新数据库并发放相应的奖励。）
					pay_success_time: this.formatTimestampToDateTime(timestamp),
					pay_add_time: this.formatTimestampToDateTime(timestamp),
					pay_no: this.payNo, // 支付渠道的订单号
					create_time: timestamp
				}

				await payOrdersCollection.add({
					user_id: userId,
					...otherParams
				})
			},
			// 将时间戳转换为指定格式的日期时间字符串
			formatTimestampToDateTime(timestamp) {
				// 判断时间戳是秒还是毫秒
				const isMilliseconds = timestamp.toString().length > 10;
				const date = new Date(isMilliseconds ? timestamp : timestamp * 1000);
		
				const year = date.getFullYear();
				const month = String(date.getMonth() + 1).padStart(2, '0'); // 月份从0开始
				const day = String(date.getDate()).padStart(2, '0');
				const hours = String(date.getHours()).padStart(2, '0');
				const minutes = String(date.getMinutes()).padStart(2, '0');
				const seconds = String(date.getSeconds()).padStart(2, '0');
		
				return `${year}-${month}-${day} ${hours}:${minutes}:${seconds}`;
				
				// 输出格式为 "2024-05-19 00:16:05"
			}

		}
	}
</script>

<style lang="scss">
	.container {
		padding: 10px;
	}

	.card {
		background-color: #333;
	}

	.uni-card-txt {
		color: #fff;
	}

	.custom-forms-item {
		display: flex;
		flex-direction: row;
		justify-content: center;
		align-items: center;
	}

	.uni-forms-item {
		margin-bottom: 12px;
		align-items: center;
	}

	.space-10 {
		width: 10px;
		height: 10px;
	}

	.vip-buttons {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 10px;
		margin-top: 10px;
	}

	.vip-button {
		border: 3px solid #e5e5e5;
		border-radius: 0;
		background-color: white;
		text-align: center;
		padding: 10px;
		cursor: pointer;
	}

	.vip-button.selected {
		border-color: black;
	}

	.price {
		font-size: 18px;
		font-weight: 700;
		color: black;
	}

	@media (min-width: 768px) {
		.vip-buttons {
			grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
			/* 自动填充，最小宽度150px */
			gap: 20px;
			/* 固定间距 */
		}

		// .vip-button {
		// 	max-width: 150px; /* 设置最大宽度 */
		// }
	}
</style>
```


violation-invitation.vue

```vue
<template>
	<view>
		
	</view>
</template>

<script>
	const db = uniCloud.database();
	const dbCmd = db.command;
	
	// 用户数据库表
	const userCollectionName = 'user-accounts'
	const userCollection = db.collection(userCollectionName)
	export default {
		data() {
			return {
				
			}
		},
		methods: {
			
		}
	}
</script>

<style>

</style>
```

user-accounts.schema.json（用户数据表字段）

```json
{
	"_id": {
		"description": "存储文档 ID（用户 ID），系统自动生成"
	},
	{
		"inviter_uid": {
		"bsonType": "string",
		"description": "邀请人ID",
		"title": "邀请人",
		"defaultValue": ""
	},
	"invite_time": {
		"bsonType": "timestamp",
		"title": "受邀时间",
		"description": "受邀时间",
		"defaultValue": 0
	},
	"my_invite_code": {
		"bsonType": "string",
		"title": "我的邀请码",
		"description": "用户自身邀请码",
		"defaultValue": ""
	},
	"device_oaid": {
		"bsonType": "string",
		"title": "设备oaid",
		"description": "设备oaid",
		"defaultValue": ""
	},
	"login_date": {
		"bsonType": "timestamp",
		"title": "最后登录时间",
		"description": "最后登录时间",
		"defaultValue": {
			"$env": "now"
		}
	},
	"login_ip": {
		"bsonType": "string",
		"title": "最后登录时 IP 地址",
		"description": "最后登录时 IP 地址",
		"defaultValue": {
			"$env": "clientIP"
		}
	},
	"register_date": {
		"bsonType": "timestamp",
		"title": "注册时间",
		"description": "注册时间",
		"defaultValue": {
			"$env": "now"
		}
	},
	"register_ip": {
		"bsonType": "string",
		"title": "注册时 IP 地址",
		"description": "注册时 IP 地址",
		"defaultValue": {
			"$env": "clientIP"
		}
	}
}
```

现在给你讲查询逻辑
1. 用“账户邀请码”查询用户信息，拿到用户id（也叫邀请人的用户id），
2. 用邀请人的用户id去查询记录，这些记录里就是被邀请的用户信息（也叫受邀者）
3. 按照受邀者的“最后登录时 IP 地址”去排列数据，就是一样的在前面，同时需按照一样的数量排序，比如192.1.18.10有5个，192.0.0.1有4个...，就是说同时按照这个相同IP的数量去降序排序






1. 再增加查询永久会员用户数量和显示永久会员的我的邀请码，水平排列，不够就换行，且点击邀请码项，就自动填充到账户邀请码输入框中并自动执行检测违规，这个需求放到<uni-collapse>折叠面板组件中
2. 查询永久会员是查询字段“vip_expire_date（会员有效期至）” >= 2366812801000 （2045-01-01 00:00:01 的时间戳）

user-accounts.schema.json（用户数据表字段）
```json
"my_invite_code": {
		"bsonType": "string",
		"title": "我的邀请码",
		"description": "用户自身邀请码",
		"defaultValue": ""
	},
"vip_expire_date": { // vip_expire_time 会更好，后续版本改名
	"bsonType": "timestamp",
	"title": "会员有效期至",
	"description": "会员有效期至",
	"defaultValue": 0 // 默认值必须是0，不然每次登录都会被覆盖，如果使用"$env": "now"
},
```


将检查过检测违规的用户的用户我的邀请码和用户id保存到本地（并附上“风险”、“正常”等状态备注）
下次启动时读取本地拿到这些用户我的邀请码后在permanentVipUsers里渲染，就是在<text>{{item.my_invite_code}}</text>后面渲染状态备注，且不同颜色标识。



1. 永久会员渲染区域（invite-code-list）应该用水平排列，不够就换行再水平排列，类似jetpack compose material3-ui 中的FlowRow组件。
2. 增加将检查过检测违规的用户的用户我的邀请码和用户id保存到本地（并附上“风险”、“正常”等状态备注）
下次启动时读取本地拿到这些用户我的邀请码后在permanentVipUsers（invite-code-list）里渲染，就是在<text>{{item.my_invite_code}}</text>后面渲染状态备注，且不同颜色标识，你可以把数据状态添加到permanentVipUsers里的每项里。使用uni.setStorage（将数据存储在本地缓存中指定的 key 中，会覆盖掉原来该 key 对应的内容，这是一个异步接口）、uni.getStorage（从本地缓存中异步获取指定 key 对应的内容）、uni.removeStorage（从本地缓存中异步移除指定 key）



1. 增加对高风险账号自动封禁，封禁就是修改用户的status字段（参考下面的用户数据表字段）
2. 增加自动对没有检查过检测违规的用户自动检查，就是提供一个按钮，作用就是自动开始检测违规，这个按钮名字你起名。

```json
{
	"_id": {
		"description": "存储文档 ID（用户 ID），系统自动生成"
	},
	{
		"inviter_uid": {
		"bsonType": "string",
		"description": "邀请人ID",
		"title": "邀请人",
		"defaultValue": ""
	},
	"invite_time": {
		"bsonType": "timestamp",
		"title": "受邀时间",
		"description": "受邀时间",
		"defaultValue": 0
	},
	"my_invite_code": {
		"bsonType": "string",
		"title": "我的邀请码",
		"description": "用户自身邀请码",
		"defaultValue": ""
	},
	"device_oaid": {
		"bsonType": "string",
		"title": "设备oaid",
		"description": "设备oaid",
		"defaultValue": ""
	},
	"vip_expire_date": { // vip_expire_time 会更好，后续版本改名
		"bsonType": "timestamp",
		"title": "会员有效期至",
		"description": "会员有效期至",
		"defaultValue": 0 // 默认值必须是0，不然每次登录都会被覆盖，如果使用"$env": "now"
	},
	"login_date": {
		"bsonType": "timestamp",
		"title": "最后登录时间",
		"description": "最后登录时间",
		"defaultValue": {
			"$env": "now"
		}
	},
	"login_ip": {
		"bsonType": "string",
		"title": "最后登录时 IP 地址",
		"description": "最后登录时 IP 地址",
		"defaultValue": {
			"$env": "clientIP"
		}
	},
	"register_date": {
		"bsonType": "timestamp",
		"title": "注册时间",
		"description": "注册时间",
		"defaultValue": {
			"$env": "now"
		}
	},
	"register_ip": {
		"bsonType": "string",
		"title": "注册时 IP 地址",
		"description": "注册时 IP 地址",
		"defaultValue": {
			"$env": "clientIP"
		}
	},
	"status": {
		"bsonType": "int",
		"description": "用户状态：0 正常 1 禁止购买会员 2 审核中 3 已被封禁",
		"title": "用户状态",
		"defaultValue": 0,
		"enum": [{
				"text": "正常",
				"value": 0
			},
			{
				"text": "禁止购买会员",
				"value": 1
			},
			// {// 暂时没有启用
			// 	"text": "审核中",
			// 	"value": 2
			// },
			{
				"text": "已被封禁",
				"value": 3
			}
		]
	}
}
```



1. 需要对相同IP数量大于等于15个的用户账户也封禁，这里指的是被邀请的，但被邀请的人IP相同数量太多了就不行，必须封号（这也是个单独的功能，因为就算是邀请朋友，也不太可能同时10个朋友都来自己家吧然后都下载软件添邀请码？不太现实吧！）
2. 需要增加类似常量PERMANENT_VIP_TIMESTAMP这种方式的去灵活配置高风险、中风险、账号IP相同数量大于等于多少个
2. 无改动的视图部分在父元素内部使用注释如此处无改动已省略等方式代替
3. 无改动的函数部分在函数内部使用注释如此处无改动已省略等方式代替


1. 还需要再permanent-vip-header中增加统计正常、中风险、高风险的用户数量显示
2. 无改动的视图部分在父元素内部使用注释如此处无改动已省略等方式代替
3. 无改动的函数部分在函数内部使用注释如此处无改动已省略等方式代替

1. 还需要在invite-code-flow-list中增加点击selectInviteCode时，则将当前的invite-code-item高亮，比如背景颜色更深或有个外边框，总之要突出高亮显示，并且当前invite-code-item高亮时需要判断是否高风险账号，是高风险账号，则在“刷新”按钮旁边显示一个“封禁此账号”的按钮，这个按钮名字起帮我起名。这个作用是手动的单独针对某高风险账号封禁处理。
2. 无改动的视图部分在父元素内部使用注释如此处无改动已省略等方式代替
3. 无改动的函数部分在函数内部使用注释如此处无改动已省略等方式代替


1. clearCheckedUsersStorage（清除检测历史）之前应该先讯问，防止误操作
2. 无改动的视图部分在父元素内部使用注释如此处无改动已省略等方式代替
3. 无改动的函数部分在函数内部使用注释如此处无改动已省略等方式代替
4. 无改动的css部分在类名内部使用注释如此处无改动已省略等方式代替






## 在登录注册接口中增加IP黑名单（用于不可注册账号和不可登录）

1. 在section-title-container下面增加个单选框过滤，用来快速切换到正常、中风险、高风险，这样找起来也快速方便。
2. 增加对所有查询的IP高风险、中风险的统计，也是用uni-collapse，我的意思是现在“IP地址分布统计”这个是单账号的，然后我现在还要在新增加个所有账号的IP地址分布统计，比如（IP: 36.143.58.252 200个账号 高风险 绑定的邀请人的邀请码数量和邀请码人的邀请码），就是ip、相同IP账户数量、风险程度、邀请人数量、所有邀请人的邀请码（比如这200个账号分了4个邀请人，那么邀请人的邀请码就是渲染4个，点击对应的邀请码也是执行selectInviteCode，方便快速查看），也需要保存到本地储存中
3. IP黑名单功能，把高风险、中风险IP和数量记录，和“IP地址分布统计”布局差不多，但此区域有快速导出黑名单IP，就是uni-popup内部元素自己画，里面有个多行输入框，把IP放里面，方便复制，如果有直接复制IP到剪切板的方法，就用复制到剪切板。
4. 无改动的视图部分在父元素内部使用注释如此处无改动已省略等方式代替
5. 无改动的函数部分在函数内部使用注释如此处无改动已省略等方式代替
6. 无改动的css部分在类名内部使用注释如此处无改动已省略等方式代替



1. 增加对即使账户邀请的人不是相同IP，但同一天邀请大于等于高风险限制数量的且注册是同一天，也要统计出来，这类账号也可能是批量注册，只不过用了更换IP软件等。
3. IP黑名单功能，把高风险、中风险IP和数量记录，和“IP地址分布统计”布局差不多，但此区域有快速导出黑名单IP，就是uni-popup内部元素自己画，里面有个多行输入框，把IP放里面，方便复制，如果有直接复制IP到剪切板的方法，就用复制到剪切板。



1. 增加对permanentVipUsers.length保存本地，并且在下一次时要和上次的对比，就是比如上次共200人，这次查询有205人，那么新增5人，也是在section-title-container内部显示

1. 需要对batchCheckViolation方法中增加个讯问，就是是否对已封禁的账号也检查（有时候也是重新检查的作用，比如新增的某功能，可能需要重新检查统计，就不能跳过已封禁账号不检查，对吧），因为现在做的是不等于已封禁



1. 在uni-card中增加个任务状态显示，如运行中....等等
```vue
<uni-card class="card" :is-shadow="false" :is-full="false" :border="true" margin="0 0 10px 0">
	<text class="uni-card-txt">违规邀请检测系统</text>
</uni-card>
```





1. 仅在批量检查违规中统计IP黑名单高风险、中风险等，因为可以在批量查询违规中的查询邀请用户的IP统计中同时记录IP风险和数量
需求我已经给你说明了，我表达能力差，希望你能看得懂和搞明白我的需求。



你还是没听懂我的意思，我现在给你说明白一些！
1. 我要的是仅在batchCheckViolation（批量检测违规）和checkViolation（检测违规）中就可以实现“全局IP风险统计”和“同一天大量邀请检测”的功能。（因为可能有这种情况，假设a邀请人邀请的50个人里ip为192.0.0.1，那么b邀请人邀请的50个人里ip也可能为192.0.0.1，统计下来就是192.0.0.1的ip共有100个风险账号且邀请人共有2个，同时还要把邀请人的登录ip也记入IP黑名单并且标识为邀请人IP黑名单中还要单独有邀请人IP黑名单，就是全局IP风险统计区域会显示两种方式，第一种大概是IP、相同IP数量、标识为邀请人，这种我想表达的大概意思是邀请人黑名单。第二种大概是IP、相同IP数量、风险等级、邀请人数量、渲染总邀请人的邀请码项）



1. 需要结合taskStatus（任务状态）在页面实时反馈啊！





### BUG和新需求说明：
1. IP统计区域（class="ip-statistics"）目前BUG显示所有相同统计IP，哪怕不是这个邀请人的邀请用户列表下也记录了，这是错的，这个区域只记录当前邀请人的邀请用户相同IP
2. globalIpStats目前的BUG会重复统计，比如我重复查询A账号多次，会重复记录A账号的邀请用户列表的相同IP数量，导致数量一致增长则是错误的统计，为了解决这个问题，你可以同时把邀请用户的邀请码也记录下来用于去重，不然的话，统计IP有BUG。
3. 需要结合taskStatus（任务状态）在页面实时反馈！



1. 还有种情况，就算不是相同IP，但在3-5天内就完成了邀请50个人，这也是不正常的，需要记录到风险为不正常中。



## 新的检测违规系统
1. 可按日历（如2025/05/18、2025/05/17等，日历选择组件），也可按今天、昨天、前天、几天前等方式，查询当日注册的所有用户用uni-table表格显示并统计IP相同数量，IP相同数量大于等于多少的风险等级，对同IP用户可全选批量封禁，也可以多选后批量封禁。


queryInvitedUsers

## 增加黑名单IP不允许注册熊多多账号


全局IP风险统计

同一天大量邀请检测区域

### 其他要求：
1. 无改动的视图使用注释如此处无改动已省略等方式代替。
2. 无改动的函数使用注释如此处无改动已省略等方式代替。
3. 无改动的css使用注释如此处无改动已省略等方式代替。
4. 只给我有改动和新增的代码，且新增的代码插入到那个位置。