# 看这里，这是今天要做的 多账号自动请求m3u8的 一些必要点（想到的方法灵感等）
 - 1.用一个新的数据库表记录所有的fsid文件（没有则先创建记录），字段有fsid、（重点字段：是否请求过了？1=请求了，0=还未请求  --- 重点，开始请求之前要查询为0的记录，在分配给每个账号对应的一个fsid，当多账号请求任务完毕时，还要再查询数据库中是否有为0的记录，或者在每次定时任务开始的第一步时，就先查询是否有为0的记录，没有的话，就先全部重置为0，再查询为0的记录，查询的数量固定为账号数，53个账号就查询53条就行。）、请求时间、请求的账号用户名。
 - - 记录请求历史：fsid：文件ID、account_id：请求账号ID、requested：用来记录该文件是否已被请求过、request_time：请求时间
 - 2.先模拟测试，在前端模拟上面说的第1点，并且要动态写入库，测试循环任务是否全部请求完了就在下次重新开始（即先重置为0再查询为0的53条为0的数据）。
 - 3.全部测试通过后才可以，这个需求可以让ai先写着看看他的思路

# 接口：请求m3u8-url文件
 - 1.需要修改为直接查询m3u8-file-url,不走一刻相册请求接口。