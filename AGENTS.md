## 需求确认与实现原则（最高约束）

- 所有业务设计必须以真实使用场景和完整数据流为依据，同时覆盖正常流程、异常流程、边界条件、失败恢复与兼容性；代码必须具备防御性和健壮性，在输入、状态和外部依赖边界进行必要校验，错误与降级行为必须明确，禁止静默产生错误数据或错误状态。
- 在需求、业务边界、交互行为、实现方案和验收标准尚未与用户达成一致前，禁止开始修改可执行代码。此阶段只允许只读调研、需求讨论以及用户明确要求的文档整理；只有用户明确确认可以开始后，才能进入实现。
- 设计与实现应保持简单、灵活、易用和易维护，避免重复代码，并优先复用项目已有能力或提取真正稳定的共享能力。复用必须结合实际场景判断：如果抽象或共享会造成额外冗余、间接层过多、可读性下降、维护成本上升，或只是为假设中的未来需求服务，则不得为了“高复用”强行抽象。
- **最高规则：修复、优化或改进 A 问题时，严禁引入 B 问题或其他行为回归。** 实施前必须明确改动边界与受影响链路，优先采用最小且完整的改动，并为相邻行为补足有针对性的回归验证；关键行为无法验证时，不得宣称完成，也不得提交。

## Codex 收尾流程与 Git 提交门禁

本节是 Codex 的项目级强制规则。凡本轮修改了可执行代码（功能实现、重构、修 bug、测试代码、构建脚本），必须在 `git commit` 前完成下面的三段流程。只修改说明文档或纯文字技能时可以不执行，但不得借“文档改动”跳过同一轮已有的代码改动。

### Claude Code 与 Codex 命令映射

- Claude Code 的 `/code-review` 对应 Codex 内置 `/review`。用户手动操作时，在 Codex Desktop、CLI 或 IDE 中执行 `/review`，选择 **Review uncommitted changes** 审查当前未提交改动。
- Codex 的 `/review` 只读取 diff 并报告问题，不会自动修改工作区；发现问题后必须回到实现流程修复。
- Codex 没有内置 `/simplify`。本项目将其定义为下面的“简化清理”阶段，由 Codex 自动执行，不要求用户手动输入命令，也不得伪称执行了不存在的 `/simplify` 命令。
- Codex 在一次实现任务内部收尾时，不必真的向输入框发送斜杠命令；必须执行与 `/review` 等价的完整 diff 审查，并按下面的格式汇报结果。用户主动执行 `/review` 时，以内置审查结果为准继续修复。

### 强制顺序

1. **第一次正确性审查（等价于 `/review`）**
   - 先用 `git status --short`、`git diff`，必要时加 `git diff --cached`，确认本轮审查范围；相关未跟踪代码文件也必须纳入。
   - 只查正确性问题：真实 bug、行为回归、边界条件、数据安全、架构越界、并发或资源泄漏、缺失的错误处理、测试缺口。
   - 按严重度给出 findings；有问题必须修复后才能进入下一阶段。没有问题也要明确记录“未发现正确性缺陷”，不能跳过或只报命令通过。

2. **简化清理（等价于 Claude Code `/simplify`）**
   - 只清理本轮改动：复用现有能力、合并重复逻辑、减少不必要分支或抽象、改善命名与可读性、移除本轮引入的无用代码。
   - 不在本阶段寻找或掩盖 bug，不改变已确认的业务行为，不顺手重构无关旧代码，不为了行数少而牺牲类型安全或可维护性。
   - 若没有安全且有价值的简化，明确记录“无需简化”；不得为了证明执行过而制造无意义改动。

3. **第二次正确性复审（再次等价于 `/review`）**
   - 对简化后的最终完整 diff 重新审查，重点确认简化没有引入新问题，并检查第一次修复是否完整。
   - 若发现问题，继续修复并重新复审，直到没有未解决的正确性 findings。

### 验证与提交限制

- 任一审查仍有未解决 findings、验证失败或因环境原因无法确认关键行为时，禁止执行 `git commit`；应先修复，确实受阻则向用户说明，不能把“有风险”当成“已通过”。
- 只有用户明确要求提交，并且上述门禁全部通过后，才允许创建 Git commit。不要因为完成代码就自行提交或推送。
- 每次代码任务的最终回复必须简要列出：第一次审查结果、简化清理内容（或无需简化）、第二次复审结果、已运行的验证命令及结果。

## 收尾流程 (finish-round)

- 回答两个反思问题（如实、具体、不套话）：
   1. 眼下你最没有把握的事情是什么？
   2. 关于当前情况，最大的遗漏是什么、我没有意识到什么？